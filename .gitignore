build/
install/
.cache/

# uv (ADR-018): uv.lock IS committed; only the virtualenv is ignored.
.venv/
*.o
*.a
*.so
*.dylib
compile_commands.json
CMakeFiles/
CMakeCache.txt
cmake_install.cmake
CTestTestfile.cmake
CMakeUserPresets.json
_deps/

# TLS test fixtures — generated locally, never committed
test/fixtures/tls/*.key
test/fixtures/tls/*.crt

# Follow-up analysis artifacts — tooling output, never committed
.followup-*.json
